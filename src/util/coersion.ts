/**
 * Coerces a CLI option value from a comma separated value string to an array.
 */
export function coerceList(value: string, dummyPrevious: any) {
  console.log('Coerce list', value, dummyPrevious);
}
